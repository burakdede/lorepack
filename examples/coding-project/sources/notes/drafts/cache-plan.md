# Draft Cache Plan

This draft suggests caching source files during runtime reads.

Do not implement it for v0.1. Runtime reads must use the active immutable build rather than the
mutable source tree.
