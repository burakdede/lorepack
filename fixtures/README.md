# fixtures

Golden test fixtures. Layout and the per-format expected-file contract are defined in issue #20 and architecture section 20.2.

- `documents/` parser fixtures
- `spreadsheets/` table fixtures
- `paths/` discovery and path-safety trees
- `expected/` committed expected outputs
