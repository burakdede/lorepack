{
  "name": "@lorepack/acceptance",
  "version": "0.0.0",
  "private": true,
  "description": "User acceptance scenarios, executed against the real lore binary.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=24.15 <25"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@lorepack/core": "workspace:*",
    "@lorepack/mcp": "workspace:*",
    "@lorepack/test-support": "workspace:*"
  }
}
