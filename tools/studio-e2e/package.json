{
  "name": "@lorepack/studio-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Studio in a real browser, against a real dev server.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "clean": "rm -rf test-results playwright-report"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "axe-core": "4.11.0"
  }
}
